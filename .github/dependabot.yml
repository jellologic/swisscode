# Keeps the SHA-pinned GitHub Actions in publish.yml and ci.yml current via
# reviewable PRs, so pinning does not mean going stale. Both workflows pin the
# same SHAs, so a bump arrives as one PR touching both.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
